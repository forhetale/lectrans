# LecTrans - 运行时依赖

# 音频采集与处理
pyaudio==0.2.14
numpy==1.26.4
pydub==0.25.1

# 本地语音识别（ASR: local）
faster-whisper==1.2.1

# 云端语音识别（ASR: azure）
azure-cognitiveservices-speech>=1.49.0

# MiMo（OpenAI 兼容接口）与 HTTP
openai==1.50.0
httpx==0.27.0

# 配置、密钥与环境变量
python-dotenv==1.0.1
keyring>=25.0.0
